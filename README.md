# labelconv

## Usage
```
index_ocr.py [-h] [--seed SEED] json_path json_db

positional arguments:
  json_path
  json_db

optional arguments:
  -h, --help   show this help message and exit
  --seed SEED
```